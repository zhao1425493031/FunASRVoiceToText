# 功能影响记录

## 会议 ASR v2（当前生产路径）

| 影响域 | 说明 |
|--------|------|
| 模型 | SenseVoice、cam++ **已移除**；改用 Qwen3-0.6B/1.7B + Pyannote Community-1 |
| 配置 | `asr_backend: meeting_qwen`；新增 `qwen_*`、`pyannote_*`；删除 `asr_model`、`meeting_spk_*` |
| 依赖 | `requirements-meeting.txt`；需 `HF_TOKEN` 与 Pyannote HF 许可 |
| VAD | FSMN-VAD（FunASR）；`vad_energy_threshold` 仅作帧级后备 |
| 协议 | WebSocket v2 **不变**；`speaker_change` 语义改为 Pyannote 时间轴纠错 |
| 性能 | CPU 延迟高于 CUDA；同一代码路径 |
| 部署 | 8766；Apache 见 [meeting/Apache部署.md](meeting/Apache部署.md) |

## 历史 v1

v1（SenseVoice + cam++）说明见 [meeting/实施总结.md](meeting/实施总结.md)（已标记废止）。
