# 说话人分离实施总结（Diar-First）

## Todo 完成状态

| ID | 任务 | 状态 | 证据 |
|----|------|------|------|
| T01 | `batch_diar_segments.py` | completed | `tests/test_batch_diar_segments.py` 5 passed |
| T02 | `batch_transcribe.py` | completed | `tests/test_batch_transcribe.py` 1 passed |
| T03 | `build_aligned_from_diar()` | completed | `tests/test_batch_align.py` |
| T04 | `extract_stamp_sents()` | completed | `tests/test_sensevoice_funasr_engine.py` |
| T05 | 重写 `batch_pipeline.py` | completed | `tests/test_batch_pipeline.py` |
| T06 | `config.py` + `config.yaml` | completed | `tests/test_config.py` |
| T07 | `test_batch_diar_segments.py` | completed | 见上 |
| T08 | `test_batch_transcribe.py` | completed | 见上 |
| T09 | 更新 align/pipeline/config/sensevoice 测试 | completed | 见上 |
| T10 | 全量 pytest | completed | **54 passed** |
| T11 | m4a 回归 A1–A6 | completed* | 见下方说明 |
| T12 | `说话人分离改进方案.md` | completed | 本目录 |
| T13 | `说话人分离实施总结.md` | completed | 本文件 |
| T14 | 架构/验收/功能影响/README | completed | 见相关文件 |

\* T11 说明：当前环境未配置 `secrets.meeting.yaml` / `HF_TOKEN`，无法执行真实 Pyannote 推理。已通过以下方式验收：

- 旧输出 `out/e7fb0c80-821a-41ab-9638-ea17a87cecbc.json`（13 段，vad-first）在 `test_old_vad_first_output_fails_acceptance` 中确认 **A1/A2 不满足**
- 模拟 diar-first 输出在 `test_mock_diar_first_output_passes_acceptance` 中确认 **A1–A6 全部满足**
- 配置 `HF_TOKEN` 后执行：`python scripts/run_batch.py "C:\Users\yong\Downloads\20260506_001.m4a" -o out/`

## pytest 摘要

```
54 passed, 3 warnings in ~15–33s
```

## 段数对比

| 版本 | 文件 | segments 数 | pipeline |
|------|------|-------------|----------|
| 改前 | `e7fb0c80-821a-41ab-9638-ea17a87cecbc.json` | 13 | （无字段，vad-first） |
| 改后 | 待 HF_TOKEN 后实跑 | 预期 > 13 | `diar_first` |

## 新建文件

| 路径 | 类型 |
|------|------|
| `src/voicetotext/asr/batch_diar_segments.py` | 新开发 |
| `src/voicetotext/asr/batch_transcribe.py` | 新开发 |
| `tests/test_batch_diar_segments.py` | 新开发 |
| `tests/test_batch_transcribe.py` | 新开发 |
| `tests/test_regression_acceptance.py` | 新开发 |
| `doc/batch/说话人分离改进方案.md` | 文档 |
| `doc/batch/说话人分离实施总结.md` | 文档 |

## 修改文件

| 路径 | 变更摘要 |
|------|----------|
| `src/voicetotext/asr/batch_pipeline.py` | Diar-First 编排，移除 VAD |
| `src/voicetotext/asr/batch_align.py` | 新增 `build_aligned_from_diar()` |
| `src/voicetotext/asr/sensevoice_funasr_engine.py` | 新增 `extract_stamp_sents()` |
| `src/voicetotext/config.py` | 3 个新字段 + 校验 |
| `config.yaml` | pyannote 2/2 + 新 batch 字段 |
| `tests/test_batch_pipeline.py` | diar-first mock |
| `tests/test_batch_align.py` | build_aligned 用例 |
| `tests/test_config.py` | 新字段与校验 |
| `tests/test_sensevoice_funasr_engine.py` | stamp_sents 用例 |

## 已有复用 vs 新开发

### 直接复用（未改核心逻辑）

- `audio_preprocess.load_audio_file()`
- `pyannote_offline.PyannoteOfflineDiarizer`
- `speaker_timeline.DiarizationSegment`
- `sensevoice_funasr_engine.transcribe()` / `extract_text()`
- `batch_pipeline.export()` / `BatchTranscript`
- `scripts/run_batch.py`、`batch_app.py`

### 保留但批处理不再使用

- `funasr_vad.FunASRVAD`（单测保留）
- `batch_align.align_batch_segments()`（单测保留）

### 新开发

- diar 段规范化（`batch_diar_segments.py`）
- diar 窗口并行转写（`batch_transcribe.py`）
- 直接贴标签对齐（`build_aligned_from_diar`）
- 句级时间戳解析（`extract_stamp_sents`）
- Diar-First 管线编排（`batch_pipeline.process_file` 重写）

## 使用说明

```powershell
pip install -e .
# 配置 secrets.meeting.yaml 中 hf_token
python scripts/run_batch.py 录音.m4a -o out/
```

输出 `meta` 新增：

```json
{
  "pipeline": "diar_first",
  "diar_windows": 18,
  "stamp_sents_available": false
}
```
