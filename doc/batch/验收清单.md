# 批处理验收清单

| ID | 要求 | 结果 | 证据 |
|----|------|------|------|
| F5 | CLI `run_batch.py input.wav -o out/` 生成 json+md+srt | pass | `batch_pipeline.export()` + `test_batch_pipeline.py` |
| F6 | REST 上传返回 job_id 与产物 | pass | `POST /api/v1/transcribe` + `test_batch_api.py` |
| F7 | Diar-First：Pyannote 分段 → 逐段 ASR → 说话人贴标 | pass | `batch_pipeline.process_file()` + `test_batch_pipeline.py` |
| F8 | 默认 language=ja，可配置 zh | pass | `config.yaml` + `test_config.py` |
| F9 | `/api/v1/summarize` 返回 501 | pass | `test_llm_stub.py` |
| F10 | 无 HF token 时 `/ready` 503 | pass | `batch_pipeline.is_ready()` + `test_batch_api` mock |
| F11 | diar 段规范化单测 | pass | `test_batch_diar_segments.py` |
| F12 | diar 窗口并行转写单测 | pass | `test_batch_transcribe.py` |
| F13 | 全量 pytest | pass | 54 passed |
| A1 | segments > 13（diar-first 回归） | pass* | `test_regression_acceptance.py` |
| A2 | 无 ≥25s 混问答单段 | pass* | 同上 |
| A3 | 日程问后「はい」非提问者 | pass* | 同上 |
| A4 | 结尾「こちらこそ」非连续 4 段同一人 | pass* | 同上 |
| A5 | Schema 不变 | pass | `test_batch_pipeline.py` |
| A6 | `meta.pipeline == diar_first` | pass | `test_batch_pipeline.py` |

\* A1–A4：自动化用模拟 diar-first 输出验证；实机 m4a 需配置 `HF_TOKEN` 后人工复验。

## 人工 UAT（部署后）

- [ ] 配置 `secrets.meeting.yaml` 中 `hf_token`
- [ ] `python scripts/run_batch.py 20260506_001.m4a -o out/` 段数 > 13
- [ ] 检查 json 中 `meta.pipeline` 为 `diar_first`
- [ ] 双人日文 wav 10min+ CLI 跑通三项产物
