# 验收清单

依据《企业日语双阶段生产》与《企业公开发布 ASR 定稿》计划。

## 阶段一（SenseVoice 公开发布定稿）

| # | 验收项 | 结果 |
|---|--------|------|
| 1 | `asr_backend=sensevoice`，`punc_model: ct-punc` 兜底 | 通过 |
| 2 | 停止/end 后 `final` 带标点（整段 ITN + ct-punc 兜底） | 待实测 |
| 3 | 流式 `partial` 无句中乱句号 | 待实测 |
| 4 | `session_pcm_max_seconds` 默认 600，超限 `session_too_long` | 通过（单测）/ 待实测 |
| 5 | PC HTTPS partial/final | 待实测 |
| 6 | 手机 HTTPS | 待实测 |
| 7 | API Key 鉴权 WS | 通过 |
| 8 | `/ready` 503/200 | 通过 |
| 9 | `POST /api/transcribe` 与 WS final 同策略 | 通过（代码） |
| 10 | pytest 全通过 | 通过（**41**） |

## 阶段二（Runtime）

| # | 验收项 | 结果 |
|---|--------|------|
| 1 | docker-compose 启动 Runtime | 通过（配置就绪） |
| 2 | runtime 模式不累积 PCM、2pass-offline → `final` | 通过 |
| 3 | PC/手机 runtime 日语/中文 | 待实测 |
| 4 | Runtime 不可达 /ready 503 | 通过 |

## 公开发布实施 Todo（e1–e11）

| ID | 内容 | 状态 |
|----|------|------|
| e1-config-pcm-cap | `session_pcm_max_seconds` 默认 600 + 全部 yaml | 完成 |
| e2-punc-restorer | `asr/punc_restorer.py` + 测试 | 完成 |
| e3-sv-utterance | `transcribe_utterance` / `finalize_utterance` | 完成 |
| e4-asr-protocol | `ASRBackend.finalize_utterance` 各后端 | 完成 |
| e5-session-pcm | `StreamSession` PCM 缓冲与 finalize | 完成 |
| e6-ws-transcribe | ws 超限；`routes_transcribe` 对齐 | 完成 |
| e7-web-final-ui | `web/app.js` final 展示与 error | 完成 |
| e8-pytest-docs | pytest + README/集成文档 | 完成 |
| e9-uat-sensevoice | 标点、无句中句号、10min 上限 | 待实测 |
| e10-uat-runtime | Runtime 2pass PC/手机 | 待实测 |
| e11-doc-release | `doc/企业公开发布实施方案.md` | 完成 |

## 双阶段骨架 Todo（历史）

| ID | 内容 | 状态 |
|----|------|------|
| p1-asr-abstraction | asr 包、base、factory | 完成 |
| p1-sensevoice-engine | SenseVoiceEngine + test | 完成 |
| p1-stream-session | stream_window + ASRBackend | 完成 |
| p2-runtime-client | runtime_client + test | 完成 |
| p2-ws-runtime-branch | ws/app runtime 分支 | 完成 |

## 自动化测试

```powershell
$env:PYTHONPATH="src"
python -m pytest tests/ -q -p no:flask
```

预期：`41 passed`
