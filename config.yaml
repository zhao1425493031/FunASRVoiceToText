# 录音文件转写 — SenseVoice + Pyannote Community-1
# 用法: python scripts/run_batch.py input.wav -o out/

host: "0.0.0.0"
port: 8767
device: "auto"
language: "ja"

asr_model: "iic/SenseVoiceSmall"
vad_model: "fsmn-vad"
trust_remote_code: true
sample_rate: 16000
log_dir: "logs"

pyannote_model: "pyannote/speaker-diarization-community-1"
pyannote_min_speakers: 1
pyannote_max_speakers: 0
pyannote_hf_token_env: "HF_TOKEN"
secrets_file: "secrets.meeting.yaml"

output_formats: [json, md, srt]
max_audio_duration_sec: 14400
batch_align_min_overlap_ms: 300
batch_max_segment_ms: 30000

api_key: "batch-dev-8n3k1q5y"

llm:
  enabled: false
  provider: stub
  model: ""
